<nav class="navbar navbar-expand-sm navbar-light bg-light border-bottom mb-4">
  <div class="container">
    <%= link_to "MeetupWebsite", root_path, class: "navbar-brand" %>

    <div class="navbar-collapse">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%#= link_to "Plans", plans_path, class: "nav-link #{"active" if request.path.start_with?("/plans")}" %>
        </li>
      </ul>
    </div>

    <% if user_signed_in? %>
      <div class="dropdown">
        <button class="btn btn-info dropdown-toggle" data-bs-toggle="dropdown" type="button">
          <%= current_user.email %>
        </button>
        <div class="dropdown-menu dropdown-menu-end">
          <%= link_to "Change password", edit_user_password_path, class: "dropdown-item" %>
          <%= link_to "Change email", edit_user_registration_path, class: "dropdown-item" %>
          <div class="dropdown-divider"></div>
          <%= link_to "Delete account", user_registration_path, data: { turbo_method: :delete }, class: "dropdown-item text-danger" %>
          <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" %>
        </div>
      </div>
    <% else %>
      <div>
        <%= link_to t('.sign_in'), new_session_path(:user), class: "btn btn-outline-primary", data: { turbo: true, turbo_frame: 'modal' } %>
        <%= link_to t('.sign_up'), new_registration_path(:user), class: "btn btn-success", data: { turbo: true, turbo_frame: 'modal' } %>
      </div>
    <% end %>
  </div>
</nav>
